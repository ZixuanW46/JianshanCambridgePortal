import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Deployed on Vercel - full Next.js features available (SSR, API Routes, etc.)
  // No static export needed
};

export default nextConfig;
